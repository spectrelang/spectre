type SxBuildConfig = {
    x: i32
    y: i32
}

pub fn main() void! = {
    val cfg: mut SxBuildConfig = @alloc(@sizeof(SxBuildConfig)) 
    @memset(cfg as ref void, 0, @sizeof(SxBuildConfig) as usize)
}
