pub fn swap[T](x: mut ref T, y: mut ref T) void = {
    val tmp = @deref(x)
    @deref(x) = @deref(y)
    @deref(y) = tmp
}

pub fn main() void = {
    val x: mut i32 = 1
    val y: mut i32 = 2
    swap[i32](@addr(x), @addr(y))
    trust @print("x: {d}\ny: {d}\n", {x, y})
}
