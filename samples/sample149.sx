pub fn add_same[T](left: T, right: T) T = {
    when T in (i32, i64, usize, isize) {
        return left + right
    }
    return left
}

pub fn main() void = {
    val total = add_same[i32](20 as i32, 22 as i32)
    trust @print("{d}\n", {total})
}
