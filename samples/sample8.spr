val std = use("std")

pub fn main() void! = {
    val x: i32 = 20

    if (x < 30) {
        std.io.print("good")
    } elif (x < 10) {
        std.io.print("what...?")
    }

    for (y = 0; y < 10; y++) {
        std.io.put_any("{d}\n", {y})
    }

//    for {
//        std.io.print("infinite loop")
//    }
}