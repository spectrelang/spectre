val stdio = use("std/stdio")
val os = use("std/os")

pub fn main() void = trust {
    val args = os.get_args()
    for arg in args {
        stdio.print("arg: {s}\n", {arg})
    }
    stdio.puts("hello")
    stdio.print("{d}\n", {@len(args)})
}
