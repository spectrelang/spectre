pub type SomeType = {
    x: i32
    y: i32
}

pub fn main() void = {
    val st: SomeType = {x: 1, y: 20}
    if true && true || true == true || st.x == 1 && st.y == 20 || true || true || true || true || true || true{
        @puts("wow")
    }
}
