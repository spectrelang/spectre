val algorithms = use("std/algorithms")

pub fn main() void = {
    val y = [12412, 123123, 12312312, 1212]
    val y_sorted = algorithms.sort[i32](y)
    for ys in y_sorted {
        trust @print("{d}\n", {ys})
    }
}
