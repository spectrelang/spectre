val std = use("std")

pub fn safe_func(x: i32, y: i32) void = {
    guarded pre {
        is_safe: x > 10 && y > 10   
    }
    trust std.stdio.put("here i go, doing some thing")
}

pub fn main() void = {
    safe_func(11, 11)
}