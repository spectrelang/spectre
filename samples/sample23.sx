val stdio = use("std/stdio")

pub fn safe_func(x: i32, y: i32) void = {
    guarded pre {
        is_safe: x > 10 && y > 10   
    }
    stdio.puts("here i go, doing some thing")
}

pub fn main() void = {
    safe_func(11 as i32, 11 as i32)
}
