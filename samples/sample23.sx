val stdio = use("std/stdio")

pub fn safe_func(x: i32, y: i32) void = {
    guarded pre {
        is_safe: x > 10 && y > 10   
    }
    trust stdio.puts("here i go, doing some thing")
}

pub fn main() void = {
    safe_func(11, 11)
}