val stdio = use("std/stdio")

pub fn main() void! = {
// old syntax, should no longer compile:
//     stdio.puts(`
// Hello, world.
// This is a multiline string.
//     `)

    stdio.puts(
        \\Hello, world.
        \\This is a multiline string.
    )
}