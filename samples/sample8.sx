val std = use("std")

pub fn main() void! = {
    val x: i32 = 20

    if (x < 30) {
        std.io.put("good")
    } elif (x < 10) {
        std.io.put("what...?")
    }

    for (y = 0; y < 10; y++) {
        std.io.print("{d}\n", {y})
    }

//    for {
//        std.io.put("infinite loop")
//    }
}