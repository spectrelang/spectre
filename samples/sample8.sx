val stdio = use("std/stdio")

pub fn main() void! = {
    val x: i32 = 20

    if (x < 30) {
        stdio.puts("good")
    } elif (x < 10) {
        stdio.puts("what...?")
    }

    for (y = 0; y < 10; y++) {
        stdio.print("{d}\n", {y})
    }

//    for {
//        std.stdio.puts("infinite loop")
//    }
}