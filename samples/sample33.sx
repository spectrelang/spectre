when darwin {
    extern type UnameResult = {
        sysname: [256]char
        nodename: [256]char
        release: [256]char
        version: [256]char
        machine: [256]char
    }
}

when linux {
    extern type UnameResult = {
        sysname: [65]char
        nodename: [65]char
        release: [65]char
        version: [65]char
        machine: [65]char
    }
}

when freebsd {
    extern type UnameResult = {
        sysname: [256]char
        nodename: [256]char
        release: [256]char
        version: [256]char
        machine: [256]char
    }
}

when openbsd {
    extern type UnameResult = {
        sysname: [256]char
        nodename: [256]char
        release: [256]char
        version: [256]char
        machine: [256]char
    }
}

when netbsd {
    extern type UnameResult = {
        sysname: [32]char
        nodename: [256]char
        release: [256]char
        version: [256]char
        machine: [256]char
    }
}

val stdio = use("std/stdio")
extern (C) fn uname(buf: ref UnameResult) i32! = "uname"

pub fn main() void! = {
    stdio.puts("hi1")

    val buf: mut UnameResult = {
        sysname: 0,
        nodename: 0,
        release: 0,
        version: 0,
        machine: 0
    }
    stdio.puts("hi2")

    val r = uname(addr(buf))
    stdio.puts("hi3")

    stdio.print("uname returned: {d}\n", {r})

    if (r == 0) {
        stdio.print("sysname: {s}\n", {buf.sysname})
        stdio.print("release: {s}\n", {buf.release})
    }
}
