val stdio = use("std/stdio")

union SomeUnion = {
    i32 | i64 | ref u8
}

fn some_function(x: SomeUnion) void! = {
    match x {
        i32 => {
            stdio.puts("hi inty int int")
        }
        i64 => {
            stdio.puts("hi bigger inty int")
        }
        else => {
            stdio.puts("hi something else")
        }
    }
}

pub fn main() void = {
    trust some_function(12)
}
