val stdio = use("std/stdio")

fn get_str() ref u8 = {
    return "hello"
}

fn identity(s: ref u8) ref u8 = {
    return s
}

pub fn main() i32 = {
    val s: ref u8 = get_str()
    val result: ref u8 = identity(s)
    stdio.print("{s}", {result})
    return 0
}
