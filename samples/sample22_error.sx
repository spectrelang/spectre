pub fn main() void! = {
    val x: mut i32 = 10
    val y: i32 = x
    // So here there are two errors, x is not ever mutated (it is not marked as ref either, meaning it cannot ever be mutated)
    // And two, the value of y is never used, so its dead code.
}