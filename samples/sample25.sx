pub fn takes_ref(buf: mut ref i32) void = {
    @deref(buf) = 42
}

val stdio = use("std/stdio")

pub fn main() void! = {
    val y: mut i32 = 30
    takes_ref(@addr(y))
    stdio.print("{d}", {y})
}
