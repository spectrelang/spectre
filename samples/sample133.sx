pub spec SomeSpec = {
    pub fn do_some_thing() i32
}

pub type (SomeSpec) SomeType = {
}

pub fn (SomeType) do_some_thing() i32 = {
    return 12
}

pub fn main() void = {
   SomeType.do_some_thing() 
}
