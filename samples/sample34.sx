val std = use("std")

fn get_platform_name() ref char = {
    return "Linux"
}

test {
    val platform = get_platform_name()
    
    match platform {
        "Windows" => {
            std.stdio.puts("Running on Windows")
        }
        "Linux" => {
            std.stdio.puts("Running on Linux")
        }
        "Darwin" => {
            std.stdio.puts("Running on macOS")
        }
    }
}
