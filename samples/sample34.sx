val std = use("std")

fn get_platform_name() ref char = {
    return "Linux"
}

test {
    val platform = get_platform_name()
    
    match platform {
        "Windows" => {
            std.stdio.put("Running on Windows")
        }
        "Linux" => {
            std.stdio.put("Running on Linux")
        }
        "Darwin" => {
            std.stdio.put("Running on macOS")
        }
    }
}
