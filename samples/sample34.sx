val stdio = use("std/stdio")

fn get_platform_name() ref char = {
    return "Linux"
}

test {
    val platform = get_platform_name()
    
    match platform {
        "Windows" => {
            stdio.puts("Running on Windows")
        }
        "Linux" => {
            stdio.puts("Running on Linux")
        }
        "Darwin" => {
            stdio.puts("Running on macOS")
        }
    }
}
